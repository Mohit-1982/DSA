//In this question i have converted a circular linked list into a singly linked list and print all elements of it.


package LinkedList;
class Node{
    int val;
    Node next;
    Node(int val){
        this.val = val;
    }
}
public class SinglytoCircularList {
    public static void main(String[] args) {
        Node a = new Node(10);
        Node b = new Node(20);
        Node c = new Node(30);
        Node d = new Node(40);
        Node e = new Node(50);
        a.next = b;
        b.next = c;
        c.next = d;
        d.next = e;
        Node temp = a;
        while(temp.next != null){
            temp = temp.next;
        }
        temp.next = a;
        temp = a;
        while(temp.next != a){
            System.out.print(temp.val + " ");
            temp = temp.next;
        }
        System.out.print(temp.val);
    }
}
